@model IEnumerable<CompanyFAQ>

<div class="card">
    <div class="card-body">
        <h5 class="card-title">CompanyFAQ List</h5>

        <a class="btn btn-success" asp-action="Create" asp-controller="CompanyFAQHome"> Create </a>
        <hr />
        <!-- Bordered Table -->
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">FAQ</th>
                    <th scope="col">Answer</th>
                    <th scope="col">Added Date</ths>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @{
                    int srno = 1;
                }
                @foreach (var temp in Model)
                {
                    <tr>
                        <th scope="row">@srno</th>
                        <td>@temp.FAQ</td>
                        <td>@temp.Answer</td>
                        <td>@temp.AddedDate</td>
                        <td>
                            <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@temp.CompanyFAQId">Edit</a>
                            |
                            <a class="btn btn-outline-primary" asp-action="Delete" asp-route-id="@temp.CompanyFAQId">Delete</a>
                        </td>
                    </tr>
                    srno++;
                }
            </tbody>
        </table>
        <!-- End Bordered Table -->



    </div>
</div>