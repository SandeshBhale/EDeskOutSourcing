@model IEnumerable<Education>

<div class="card">
    <div class="card-body">
        <h5 class="card-title">Education List</h5>

        <a class="btn btn-success" asp-action="Create" asp-controller="EducationHome"> Create </a>
        <hr />
        <!-- Bordered Table -->
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Education Name</th>
                    <th scope="col">Education Type</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @{
                    int srno = 1;
                }
                @foreach (var temp in Model)
                {
                    <tr>
                        <th scope="row">@srno</th>
                        <td>@temp.EducationName</td>
                        <td>@temp.EducationType</td>
                        <td>
                            <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@temp.EducationId">Edit</a>
                            |
                            <a class="btn btn-outline-primary" asp-action="Delete" asp-route-id="@temp.EducationId">Delete</a>
                        </td>
                    </tr>
                    srno++;
                }
            </tbody>
        </table>
        <!-- End Bordered Table -->



    </div>
</div>