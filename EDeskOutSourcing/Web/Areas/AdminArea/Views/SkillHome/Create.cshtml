@model Skill
<div class="card">
    <div class="card-header d-flex justify-content-between">
        <div class="header-title">
            <h4 class="card-title">Add State</h4>
        </div>
    </div>
    <div>
        @if (ViewBag.Message != null)
        {
            <div class="alert alert-info">@ViewBag.Message</div>
        }

    </div>
    <div class="card-body">
        <form class="form-horizontal" asp-action="Create" method="post">
            <div class="form-group row">
                <label class="control-label" for="email1">Skill Name : </label>
                <div class="col-sm-12">
                    <input asp-for="SkillName" class="form-control" />
                    <span asp-validation-for="SkillName" class="text-danger"></span>
                </div>
            </div>
            <div class="col-12">
                <label class="form-label">Skill Category</label>
                <select class="form-select" asp-for="SkillCategoryId" asp-items="@ViewBag.SkillCategoryId">
                    <option> Select Skill Category</option>
                </select>
                <span asp-validation-for="SkillCategoryId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-success">Create</button>
            </div>
        </form>
    </div>
</div>